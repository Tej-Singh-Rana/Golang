// goroutines need to communicate and they do safely via channels. goroutine --- channel --- goroutine
// unbuffered & buffered
// unbuffered channel --> channel-name := make(chan int) no size initialize.
// buffered channel --> channel-name := make(chan int, 5) size available.

package main

import "fmt"

func main(){
				

}